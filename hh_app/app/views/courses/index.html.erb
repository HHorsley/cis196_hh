<head>
</head>
<body>
<table>
	<tr>
		<td><strong>Department</strong></td>
		<td><strong>Topic</strong></td>
		<td><strong>Rough Description</strong></td>
		<td><strong>Desired Instructor</strong></td>
		<td><strong>Idea Creator</strong></td>
	</tr>
		<% @courses.each do |course| %>
	<tr>
		<td><%= course.department %></td>
		<td><%= course.topic %></td>
		<td><%= course.description %></td>
		<td><%= course.desired_instructor %></td>
		<%if course.user == nil %>
			<td>N/A</td>
		<% else%>
			<td><%=  course.user.name %></td>
		<%end%>
		<td><%= link_to "show course", course_path(course.id) %></td>
		<td><%= link_to "edit course", edit_course_path(course.id) %></td>
		<td><%= button_to "destroy course", course, :confirm => "Are you sure you want to destroy this course?", method: :delete %></td>
	</tr>
	<% end %>
</table>
</body>
